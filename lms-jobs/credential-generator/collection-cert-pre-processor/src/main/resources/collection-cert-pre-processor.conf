include "base-config.conf"

# Set redis.enabled = false to disable Redis caching.
# When disabled, content and course metadata lookups fall back to the Content API automatically.
redis {
  enabled = false
}
